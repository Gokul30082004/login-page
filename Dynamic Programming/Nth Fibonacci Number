#include<bits/stdc++.h>
using namespace std;

int f(int a){
        if(a==1 || a==2){
                return 1;
        }
        return f(a-1)+f(a-2);
}

int main()
{
        int a;
       cin>>a;
       cout<<f(a);
}
